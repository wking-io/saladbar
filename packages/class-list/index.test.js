import test from 'tape';
import classList from './index';
import Element from 'salad-bar-utils/element';

test('classList adds a class to an element', assert => {
  assert.equal(true, true);
  assert.end();
});
